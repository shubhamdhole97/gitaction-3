---
- name: Create users with port-specific SSH access
  hosts: all
  become: true
  vars:
    user_ports:
      - { name: "alice", port: 2221 }
      - { name: "bob", port: 2222 }
      - { name: "charlie", port: 2223 }
      - { name: "shubham", port: 2224 }

  tasks:
    - name: Ensure user exists
      user:
        name: "{{ item.name }}"
        state: present
        shell: /bin/bash
        create_home: yes
      loop: "{{ user_ports }}"

    - name: Ensure .ssh directory exists
      file:
        path: "/home/{{ item.name }}/.ssh"
        state: directory
        mode: '0700'
        owner: "{{ item.name }}"
        group: "{{ item.name }}"
      loop: "{{ user_ports }}"

    - name: Add shared public key to authorized_keys
      copy:
        src: keys/shared.pub
        dest: "/home/{{ item.name }}/.ssh/authorized_keys"
        owner: "{{ item.name }}"
        group: "{{ item.name }}"
        mode: '0600'
        remote_src: no
      loop: "{{ user_ports }}"

    - name: Copy base sshd_config template
      copy:
        src: /etc/ssh/sshd_config
        dest: "/etc/ssh/sshd_config_{{ item.port }}"
        remote_src: yes
      loop: "{{ user_ports }}"

    - name: Customize sshd_config for each user
      lineinfile:
        path: "/etc/ssh/sshd_config_{{ item.port }}"
        regexp: '^Port '
        line: "Port {{ item.port }}"
        state: present
      loop: "{{ user_ports }}"

    - name: Set AllowUsers in each sshd_config
      lineinfile:
        path: "/etc/ssh/sshd_config_{{ item.port }}"
        regexp: '^AllowUsers '
        line: "AllowUsers {{ item.name }}"
        state: present
        insertafter: EOF
      loop: "{{ user_ports }}"

    - name: Launch sshd on custom port
      shell: |
        /usr/sbin/sshd -f /etc/ssh/sshd_config_{{ item.port }}
      args:
        executable: /bin/bash
      loop: "{{ user_ports }}"
